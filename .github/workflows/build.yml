name: Build & Test

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore "LandRe-Adjustment Tool.sln"

      - name: Build
        run: dotnet build "LandRe-Adjustment Tool.sln" --no-restore --configuration Release

      - name: Test
        run: dotnet test "LandReadjustment.Tests/LandReadjustment.Tests.csproj" --no-build --configuration Release --verbosity normal
