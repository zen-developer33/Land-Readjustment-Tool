# Copilot Instructions

## Project Guidelines
- User prefers numeric display precision behavior to be controlled by constants so changing a single constant updates formatting app-wide (e.g., trailing zero precision).